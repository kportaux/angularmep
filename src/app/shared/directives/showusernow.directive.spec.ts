import { ShowusernowDirective } from './showusernow.directive';

describe('ShowusernowDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowusernowDirective();
    expect(directive).toBeTruthy();
  });
});
